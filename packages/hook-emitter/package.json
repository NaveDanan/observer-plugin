{
  "name": "@observer-ai/hook-emitter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "observer-emit": "./dist/emit.js"
  },
  "main": "./dist/emit.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').chmodSync('dist/emit.js', 0o755)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
