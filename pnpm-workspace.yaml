packages:
  - "apps/*"
  - "packages/*"
  - "integrations/*"

# esbuild ships prebuilt binaries and needs its install script to select one.
allowBuilds:
  esbuild: true

onlyBuiltDependencies:
  - esbuild
