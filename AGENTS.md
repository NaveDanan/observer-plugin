## Agent skills

### Issue tracker

Work is tracked as local Markdown under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the configured local status values. See `docs/agents/triage-labels.md`.

### Domain docs

This monorepo uses context documents per app and package. See `docs/agents/domain.md`.
